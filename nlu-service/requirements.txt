# NLU service (SPEC §11 bước 2.3) — suy luận CPU, không cần datasets/evaluate như lúc train.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers>=4.40
py_vncorenlp>=0.1.4
fastapi>=0.111
uvicorn>=0.30
# Embedding cho semantic rerank (GĐ3, SPEC §12.1) — vietnamese-sbert.
sentence-transformers>=3.0
